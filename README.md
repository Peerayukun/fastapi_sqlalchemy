# fastapi_sqlalchemy

#### install requirements
```
pip install -r requirements.txr
```

#### requirements for development
```
pip install -r requirements.txr
```

#### setup config to file .env


#### runserver
```
uvicorn main:app --reload
```

### unit test

#### run all unit test
```
python -m unittest
```

#### run unit test
```
python -m unittest discover -s tests.cases.<app_name>
```
